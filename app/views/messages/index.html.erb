<h1>メッセージ一覧</h1>
<% @messages.each do |message| %>

名前:
<%= message.name %>

内容:
<%= message.body %>

<br>
<% end %>

<%= form_for(Message.new) do |f| %>

name: 

<%= f.text_field :name %>

opinion:

<%= f.text_field :body %>

<%= f.submit %>

<% end %>

